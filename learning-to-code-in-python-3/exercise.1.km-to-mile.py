#!/usr/bin/env python3

distance_in_km = float(input("What is the distance in kilometers?: "))
distance_in_miles = distance_in_km / 1.60934

print(f"{distance_in_km} km = {round(distance_in_miles)} mi")